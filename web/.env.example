# Gerado com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Gerado com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Senha forte para o banco
POSTGRES_PASSWORD=

# Secret para o endpoint admin — use token_hex(24)
ADMIN_SECRET=

# Master key do LiteLLM na VPS
LITELLM_MASTER_KEY=
